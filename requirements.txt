fastapi>=0.115
uvicorn[standard]>=0.32
pydantic-settings>=2.5
python-json-logger>=2.0
pytest>=8.3
httpx>=0.27
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
passlib[bcrypt]>=1.7
# passlib 1.7.4 (última versión, sin releases desde 2020) rompe con bcrypt>=4.1:
# el módulo perdió el atributo `__about__` que passlib usa para detectar la
# versión, y además bcrypt>=4.1 dejó de aceptar en silencio contraseñas de más
# de 72 bytes en el autotest interno de passlib. Fijado tras reproducirlo.
bcrypt<4.1,>=4.0
pyjwt>=2.9
openpyxl>=3.1
