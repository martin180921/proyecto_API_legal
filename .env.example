# Copiar a .env y rellenar. Nunca commitear .env real (ver Reglas de trabajo del
# desarrollador único: "nunca copies credenciales a la bóveda" aplica también aquí).

APP_ENV=local
LOG_LEVEL=INFO

# Se activan en la Semana 2, al llegar el modelo de datos y la autenticación:
# DATABASE_URL=postgresql://usuario:password@localhost:5432/api_legal
# SECRET_KEY=
